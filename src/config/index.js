const NODE_ENV = 'development';
const RPC_URL = 'https://eth.public-rpc.com';
const COIN_GECKO_SIMPLE_PRICE = 'https://api.coingecko.com/api/v3/simple/price';

export const config = {
  RPC_URL,
  NODE_ENV,
  COIN_GECKO_SIMPLE_PRICE,
};
